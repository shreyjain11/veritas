import { Viewer } from "../components/viewer/Viewer";

// The report viewer is the page for now; the landing page comes next and will move the
// viewer to /report.
export default function Page() {
  return <Viewer />;
}
